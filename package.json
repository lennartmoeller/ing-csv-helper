{
  "scripts": {
    "typecheck": "tsc --noEmit",
    "bundle": "esbuild src/index.ts --bundle --platform=browser --target=ES2020 --minify --outdir=dist",
    "build": "npm run typecheck && npm run bundle"
  },
  "devDependencies": {
    "esbuild": "^0.25.2",
    "typescript": "^5.8.3",
    "@types/chrome": "^0.0.322",
    "@types/node": "^22.15.18"
  }
}
